html files are called templates 
Flask framework will look for templates in the templates folder
from flask import render_template

we can edit webpage by going in chrome and use developer tool and in the java script console write document.body.contentEditable=true